#include <stdio.h>
/**
 * main - main entry point
 * Description: 'print num xero to nine'
 * Return: always 0
 */
int main(void)
{
	int g;

	for (g = 0; g <= 9; g++)
	{
		printf("%d", g);
	}
	printf("\n");
	return (0);
}
